import React from "react";

const Heading = ({ title }) => {
  return (
    <div className="font-dm text-[20px] font-bold text-[#262626] md:text-[39px]">
      {title}
    </div>
  );
};

export default Heading;
